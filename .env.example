# Copy to .env and fill in. .env is gitignored.
# UPenn WRDS credentials (https://wrds-www.wharton.upenn.edu/)
WRDS_USERNAME=your_wrds_username
WRDS_PASSWORD=your_wrds_password

# Optional: pin the cache directory (defaults to ./data_cache)
# CB_CACHE_DIR=./data_cache
