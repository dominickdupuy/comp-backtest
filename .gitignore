# Secrets
.env
.pgpass
*.pgpass

# Data cache (large; regenerated from WRDS, tracked via Git LFS)
*.parquet
!data_cache/
!data_cache/*.parquet
# transient live-API caches (CT.gov / EDGAR catalyst pulls; regenerable)
data_cache/*.json

# Results (derived artifacts; regenerated by scripts)
results/*.html
results/*.png
results/*.csv
results/*.json
results/*.txt

# Stray git-lfs hooks dir from a bad core.hookspath
dev/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/
.pytest_cache/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db
